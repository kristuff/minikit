/* Kristuff.Minikit.Ratings */
(function (window, undefined) {
    'use strict';
    var progressbar = (function () {

        var create = function() {

        };


        /* Public methods */
        return {
            create: create,
        };
    })();

    // register
    window.Minikit.Progressbar = progressbar;
})(window);
